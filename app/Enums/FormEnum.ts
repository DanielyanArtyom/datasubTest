export enum FormFieldType {
  Masked = "inputMask",
  Standard = "inputField",
}

export enum FormsElements {
  Card_Number = "card_number",
  Card_Expiration_date = "card_expirationDate",
  Card_CVC = "card_cvc",
  Amount = "amount",
}
